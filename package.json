{
  "name": "fullstack-ecommerce-redijson-redisearch",
  "version": "1.0.0",
  "description": "fullstack ecommerce filter sort with redijson and redisearch!",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "client:dev": "npm run dev --workspace=client",
    "server:dev": "npm run start:dev --workspace=server",
    "start": "concurrently 'npm:server:dev' 'npm:client:dev'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "ts",
    "react",
    "nestjs"
  ],
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^7.0.0"
  }
}